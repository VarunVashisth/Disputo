DROP YOUR PERSONA PNG FILES HERE.

Expected filenames (must match exactly):
  aria.png   → ARIA (Libertarian)
  marx.png   → MARX (Socialist)
  sage.png   → SAGE (Stoic Philosopher)
  nova.png   → NOVA (Techno-Optimist)
  human.png  → YOU (Human player) — optional, silhouette used if missing

Portrait orientation works best (taller than wide, face in upper 60%).
Any resolution is fine — canvas scales them automatically.

These files are served from /public so they load as:
  /personas/aria.png  (no import, no CORS, works offline)
